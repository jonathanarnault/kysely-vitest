{
	"name": "@kysely-vitest/postgres-test",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"repository": {
		"type": "https",
		"url": "https://github.com/jonathanarnault/kysely-vitest"
	},
	"scripts": {
		"build": "tsc -p tsconfig.lib.json",
		"typecheck": "tsc --noEmit -p tsconfig.lib.json -p tsconfig.test.json",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"@kysely-vitest/postgres": "workspace:^",
		"postgres": "catalog:"
	},
	"devDependencies": {
		"kysely-postgres-js": "catalog:"
	}
}
