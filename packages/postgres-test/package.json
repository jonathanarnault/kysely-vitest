{
	"name": "@kysely-vitest/postgres-test",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "tsc -p tsconfig.lib.json",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"@kysely-vitest/postgres": "workspace:^",
		"postgres": "catalog:"
	},
	"devDependencies": {
		"kysely-postgres-js": "catalog:"
	}
}
